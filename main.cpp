#include "compiler.h"

int main(void) {
	compiler* new_compiler = new compiler();
	new_compiler->start();
}

/*
#include "compiler.h"

int main(void) {
	compiler* new_compiler = new compiler();
	new_compiler->start();
}
*/